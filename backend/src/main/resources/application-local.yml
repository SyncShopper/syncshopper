spring:
  docker:
    compose:
      enabled: false

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/syncshopper?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: potato

  jackson:
    time-zone: Asia/Seoul

server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
