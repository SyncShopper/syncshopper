<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.syncshopper.mapper.AiAnalysisLogMapper">

    <insert id="insertAiAnalysisLog"
            parameterType="com.syncshopper.domain.detection.AiAnalysisLog"
            useGeneratedKeys="true"
            keyProperty="logId">
        INSERT INTO ai_analysis_logs (
            detection_id,
            api_provider,
            request_payload,
            response_payload,
            success_yn,
            error_message,
            latency_ms
        )
        VALUES (
            #{detectionId},
            #{apiProvider},
            #{requestPayload},
            #{responsePayload},
            #{successYn},
            #{errorMessage},
            #{latencyMs}
        )
    </insert>

</mapper>
